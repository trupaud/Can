#include <stdio.h>
#include "pico/stdlib.h"

int GPIO6 = 6;
int GPIO7 = 7;
int GPIO8 = 8;
int GPIO9 = 9;

int main() {
  pinMode(GPIO6, OUTPUT);
  pinMode(GPIO7, OUTPUT);
  pinMode(GPIO8, OUTPUT);
  pinMode(GPIO9, OUTPUT);
}

void loop() {
  void Timer1() 
  TCCR1A = 0;  
  TCCR1B = 0;
}
  {OCR1A = (F_CPU / 1000) * TIEMPO_A / 64 - 1;
   TCCR1B |= (1 << CS11) | (1 << CS10);
   TIMSK1 |= (1 << OCIE1A);
  }
   
   void inicializar() {
     configurarTimer1();
     sei();
     
int main() }  
  while (1) {
    return 0;
}
int main() {
  inicializar();
}
  while (1) {
  }
   return 0;
   ISR(TIMER1_COMPA_vect) {
   }
   ISR(TIMER1_COMPB_vect) {
   }
